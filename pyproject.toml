[project]
name = "week2-data-work"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pandas", "pyarrow"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bootcamp_data"]